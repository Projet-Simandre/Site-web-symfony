{% extends "base.html.twig" %} {# formulaire de création de compte #}

{% block body %}
	<div class="my-5"></div>
	<h1 class="text-center pt-5">Créer un compte</h1>
	<br>
	<div class="d-flex justify-content-center">
		{{form_start(form)}}
		{{form_rest(form)}}

		<input type="submit" value="Créer" class="btn btn-success">

		{{form_end(form)}}
	</div>
{% endblock %}
